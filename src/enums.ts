export enum DelegationPotential {
  ONLY_ME = 'Only I Can Do This',
  SOMEONE_ELSE = 'Someone Else Could Do This',
  ELIMINATE = 'Could Be Eliminated',
}

export enum Alignment {
  ALIGNED = 'Aligned',
  DISRUPTION = 'Disruption',
}
